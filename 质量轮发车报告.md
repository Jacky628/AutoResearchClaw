# 质量轮发车前报告 — 清理 / 全量参数 / 初始状态

> 日期：2026-06-18（**2026-06-21 更新：A 重设 — 5-tag 忠实词表 + adherence 主度量 + 40% 前缀 + 配对分析**）
> Run：`artifacts/rc-20260605-140117-fdedd0`（CAD-LLM / FORGE 论文，质量轮 = 为出可发表数据的全量轮）
> 沙盒：`stage-12/runs/sandbox/_project_1`（含 `main.py` + `paired_analysis.py` 离线分析工具）

---

## 1. 清理完成：沙盒回到纯初始态

`_project_1` 只剩：`main.py`（含全部修复）+ `.venv` + provision 文件（`requirements.txt` / `.requirements.filtered1.txt` / `provision_log.txt` / `setup.py` / `deepcad_transpiler.py` / `experiment_harness.py`）。

无 checkpoint / 结果 / 日志 / 临时探针残留。GPU 空闲、git 干净。

---

## 2. 实验参数（全量配置）

### 模型 / 训练

| 参数 | 值 |
|---|---|
| base_model | Qwen/Qwen2.5-Coder-1.5B |
| LoRA | r=16, α=32, dropout=0.05 |
| SFT | lr=2e-4, batch=4, grad_accum=8, **epochs=3**, **max_seq_length=1024** |
| GRPO | lr=5e-7, **steps=100**, num_generations=4, **grad_accum=1**, **max_completion_length=768** |
| proxy 分类器 | offline_samples=500, train_epochs=10 |
| eval | eval_subset_size=100, **eval_prefix_fraction=0.40** |
| 设计意图 token | **num_constraint_tokens=5** |
| **主度量** | **adherence**（F1: 生成程序特征 vs GT 特征）；validity 次要 |

### 设计意图 token（5 个，2026-06-21 决定 A：transpiler 忠实特征，从 GT **代码**检测）

`<CIRCLE> <NGON> <THIN> <TALL> <MULTI_PART>`

> **演进**：7 几何约束(近常量→null) → 06-18 重设 12 个 design-intent tag → **06-21 决定 A 收缩到 5 个**。原因:`deepcad_transpiler` 故意简化(只取首 loop/首 face、cut 零深度)→ 丢孔/多轮廓/cut/arc/concentric → 那些 tag 在训练目标代码里是幻影。只保留 transpiler 忠实 + 可靠检测的 5 个;header 改从 **GT 代码**派生(detect_features,非名义 JSON)。验证分布:16 组合/熵 3.54/空集 13%。spec/plan 见 `docs/superpowers/{specs,plans}/2026-06-21-transpiler-faithful-adherence*`。修 transpiler(B)留下一版。
>
> **5 特征签名全在程序后半段**(MULTI_PART~52% / NGON~60% / CIRCLE~74% / THIN-TALL.extrude~87% 中位)→ 前缀(前 40%)永不泄露 → header 始终有边际信息(#3"前缀冗余"实证为伪问题)。

### 预算

- `total_budget_seconds = 252000`（70h）× `budget_fraction = 0.80` → **训练停在 56h**
- 两处一致：main.py `total_budget_seconds` == config.arc.yaml `time_budget_sec` == 252000

---

## 3. 实验条件：8 个条件 × seeds（共 14 个 run）

| 条件 | seeds | 角色 |
|---|---|---|
| `deepcad_scratch_transformer` | [0,1] | 基线（byte 级 from-scratch transformer） |
| `qwen25coder_sft_no_constraints` | [0,1] | H2 baseline（无约束 header） |
| `qwen25coder_sft_constraint_text` | [0,1] | H2 对照（约束当纯文本 header） |
| `qwen25coder_sft_constraint_tokens` | [0,1] | **H2 novelty 臂**（约束当真 token header） |
| `constraint_tokens_masked_at_inference` | [0,1] | H2 用法证据（训练带 token，eval 移除 header） |
| `grpo_proxy_reward_classifier` | [0,1] | 廉价几何奖励（proxy 全特征） |
| `grpo_hidden_state_only_proxy` | [0] | 廉价几何奖励（proxy hidden-only） |
| `grpo_oracle_reward_reference` | [0] | 廉价几何奖励（oracle 参照） |

**约束臂 (train_mode, eval_mode)**：no_constraints=(none,none) / constraint_text=(text,text) / constraint_tokens=(token,token) / masked=(token,none)

**H2 核心比较（在 adherence 轴上，配对检验）**：`constraint_tokens > constraint_text`（同一份设计意图规格，差异纯来自 token vs 文本表示）；`> masked`（模型确实在用可见 header）；`> no_constraints`。四臂 eval **同一批样本** → `paired_analysis.py` 做**逐样本配对 + Wilcoxon signed-rank**（消样本方差、power 暴涨，缓解小规模噪声 #4），非比较带噪均值。

**GRPO 三条件**从 `no_constraints` SFT 起步（干净归因，避免与约束 tokenization 耦合 + 词表 resize 纠缠）。

---

## 4. 关键修复全部在位（产物 + 生成器两侧）

| 修复 | 说明 |
|---|---|
| pad≠eos（`<\|fim_pad\|>`） | SFT collator 按值 mask，pad==eos 会屏蔽真 EOS label → 学不会停。改独立 pad token |
| eval + reward 拼回 prompt（3 处） | completion 无头，须 `full = prompt + completion` 再提取，否则恒零 validity / 死奖励 |
| reward canary | GRPO 训练前确定性自检 reward 接线（full vs headless），防死奖励烧时间 |
| **设计意图 conditioning + A 收缩** | 低熵几何约束→12 tag→**A：5 个 transpiler 忠实特征**;header 从 GT 代码派生;warm-start(词义播种,去 token 冷启动混淆);4 臂 |
| **adherence 主度量 + 配对分析** | validity 盲区(测有效≠测符合意图)→ adherence(F1 生成 vs GT 特征)为主;per-sample 落盘 `adherence_samples.jsonl` + `paired_analysis.py` 配对 Wilcoxon |
| **数据去泄漏** | DeepCAD test 与 train 重叠 30%精确/40%含噪 → load_deepcad 按 exact+loose 哈希去泄漏(15848→9442 干净留出) |
| **GRPO grad_accum=1** | TRL 默认 8 → 8× 慢；修后 725s → 242s/step |
| est_peak 正确分片 / grpo_hidden 独立 ckpt | GRPO 单卡 OOM→必须分片;grpo_hidden 用独立 ckpt 防消融失效 |
| in-training canary 精准化 / numpy 序列化兜底 | canary 仅 validity==0 且 eos_frac==0 才 abort;results json.dumps 加 numpy default 防 ~47h 后崩 |
| token 臂假零修复 / 前缀 40% | extract 剥 `<CONSTRAINTS>` 非法 header(否则 token 臂恒0、H2 反转);前缀撤回 40%(特征全在后半段,不泄露)|

生成器（durable，进 git）commit：`464804d` … `f304c93` → `9946c92`(numpy) → `f83c3c6`(dedup) → `cfd9854`(token-strip) → `96239d6`(warm-start) → `5ee0892`(adherence+A) → `cf9da1b`(配对)（共 18 个）。产物 main.py 手改同步（artifact 不入库）。

---

## 5. 预期运行 + 早警设施

**预测 ≈47h（区间 45-50h）** —— 2026-06-21 A 状态，实测步时(SFT 8s/step·GRPO 242s/step 分片·生成 7.7s/样本)外推。A 改动时长中性(dedup 哈希集 +~2min 一次性、40% 前缀略快 eval、adherence/落盘 可忽略)：

| 阶段 | 明细 | 时长 |
|---|---|---|
| 数据加载 + dedup 哈希集 | 一次性 ~2min | ~0.1h |
| scratch ×2 | byte 训 + eval | ~1h |
| **SFT ×6**(no_constraints/text/tokens × 2 seed) | 6 × 2.08h(939 步 ×8s) | **~12.5h** |
| proxy 采集 ×2 | 2 × 1.07h(500 ×7.7s) | ~2.1h |
| **GRPO ×4**(proxy×2 + hidden + oracle) | 4 × 6.72h(100 步 ×242s 分片) | **~26.9h** |
| eval ×12(非 scratch) | 12 × 0.21h(100 ×7.7s) | ~2.5h |
| 模型加载×14 + oracle 冷启 + proxy 训 + 开销 | | ~2h |
| **总计** | | **≈47h** |

STOP 在 56h → **~9h 余量 + 14h 收尾缓冲(56→70h 上限)**，宽松进得去。
**最大变量 = GRPO(占 ~57%)**：242s/step 是实测基准;若久跑 VRAM 碎片使分片漂到 ~270s,GRPO +~3h → 总 ~50h(仍稳进 56h)。兜底:per-step 预算优雅降级 / per-condition try-except / 条件级可续跑。

**早警/可观测**：in-training canary（epoch 1）/ reward canary（GRPO 前）/ dead-reward 守卫 / 时间预算优雅降级 / progress.json 心跳 / **每样本 adherence 落盘 `adherence_samples.jsonl`**（intended/produced 特征 + recall/precision/valid）。**run 后跑 `paired_analysis.py adherence_samples.jsonl`** 出 H2 配对结果（tokens-vs-text/masked/none，Wilcoxon p）。整合 smoke 多次端到端验过 8/8 条件 0 崩溃（最后一次含 A+adherence+配对+40%前缀全部修复）。

---

## 6. 发车命令（直接 nohup）

```bash
cd /root/AutoResearchClaw/artifacts/rc-20260605-140117-fdedd0/stage-12/runs/sandbox/_project_1
nohup env PYTHONUNBUFFERED=1 .venv/bin/python main.py > quality_run.log 2>&1 &
```

监控：progress.json 心跳 / canary / 各条件落盘 / traceback，按整合 smoke 节奏巡检。
