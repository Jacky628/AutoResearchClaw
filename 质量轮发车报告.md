# 质量轮发车前报告 — 清理 / 全量参数 / 初始状态

> 日期：2026-06-18
> Run：`artifacts/rc-20260605-140117-fdedd0`（CAD-LLM / FORGE 论文，质量轮 = 为出可发表数据的全量轮）
> 沙盒：`stage-12/runs/sandbox/_project_1`

---

## 1. 清理完成：沙盒回到纯初始态

`_project_1` 只剩：`main.py`（含全部修复）+ `.venv` + provision 文件（`requirements.txt` / `.requirements.filtered1.txt` / `provision_log.txt` / `setup.py` / `deepcad_transpiler.py` / `experiment_harness.py`）。

无 checkpoint / 结果 / 日志 / 临时探针残留。GPU 空闲、git 干净。

---

## 2. 实验参数（全量配置）

### 模型 / 训练

| 参数 | 值 |
|---|---|
| base_model | Qwen/Qwen2.5-Coder-1.5B |
| LoRA | r=16, α=32, dropout=0.05 |
| SFT | lr=2e-4, batch=4, grad_accum=8, **epochs=3**, **max_seq_length=1024** |
| GRPO | lr=5e-7, **steps=100**, num_generations=4, **grad_accum=1**, **max_completion_length=768** |
| proxy 分类器 | offline_samples=500, train_epochs=10 |
| eval | eval_subset_size=100 |
| 设计意图 token | num_constraint_tokens=12 |

### 设计意图 token（12 个，2026-06-18 重设：从 GT operation+topology 推导）

`<CUT> <JOIN> <TWO_SIDED> <MULTI_PART> <MULTI_PROFILE> <HOLE> <CIRCLE> <ARC> <THIN> <TALL> <NGON> <CONCENTRIC>`

> 旧版 7 个几何约束（PARALLEL/H/V/PERP/...）在 DeepCAD 轴对齐棱柱上近常量（57% 同一串）→ H2 大概率 null；已重设为高熵设计意图 conditioning（88 组合/熵 5.48 bits/空集 10.8%）。详见 `docs/superpowers/specs/2026-06-18-design-intent-conditioning-design.md`。

### 预算

- `total_budget_seconds = 252000`（70h）× `budget_fraction = 0.80` → **训练停在 56h**
- 两处一致：main.py `total_budget_seconds` == config.arc.yaml `time_budget_sec` == 252000

---

## 3. 实验条件：8 个条件 × seeds（共 14 个 run）

| 条件 | seeds | 角色 |
|---|---|---|
| `deepcad_scratch_transformer` | [0,1] | 基线（byte 级 from-scratch transformer） |
| `qwen25coder_sft_no_constraints` | [0,1] | H2 baseline（无约束 header） |
| `qwen25coder_sft_constraint_text` | [0,1] | H2 对照（约束当纯文本 header） |
| `qwen25coder_sft_constraint_tokens` | [0,1] | **H2 novelty 臂**（约束当真 token header） |
| `constraint_tokens_masked_at_inference` | [0,1] | H2 用法证据（训练带 token，eval 移除 header） |
| `grpo_proxy_reward_classifier` | [0,1] | 廉价几何奖励（proxy 全特征） |
| `grpo_hidden_state_only_proxy` | [0] | 廉价几何奖励（proxy hidden-only） |
| `grpo_oracle_reward_reference` | [0] | 廉价几何奖励（oracle 参照） |

**约束臂 (train_mode, eval_mode)**：no_constraints=(none,none) / constraint_text=(text,text) / constraint_tokens=(token,token) / masked=(token,none)

**H2 核心比较**：`constraint_tokens > constraint_text`（同一份 GT 几何推导的约束规格，差异纯来自 token vs 文本表示）；`> masked`（模型确实在用可见约束）；`> no_constraints`（约束规格有用）。

**GRPO 三条件**从 `no_constraints` SFT 起步（干净归因，避免与约束 tokenization 耦合 + 词表 resize 纠缠）。

---

## 4. 关键修复全部在位（产物 + 生成器两侧）

| 修复 | 说明 |
|---|---|
| pad≠eos（`<\|fim_pad\|>`） | SFT collator 按值 mask，pad==eos 会屏蔽真 EOS label → 学不会停。改独立 pad token |
| eval + reward 拼回 prompt（3 处） | completion 无头，须 `full = prompt + completion` 再提取，否则恒零 validity / 死奖励 |
| reward canary | GRPO 训练前确定性自检 reward 接线（full vs headless），防死奖励烧时间 |
| **设计意图 conditioning（H2 重设）** | 低熵几何约束（57% 同一串→null）→ 12 个 design-intent tag（add_tokens+resize+只训新行 + 4 臂）；88 组合/熵 5.48 bits |
| **GRPO grad_accum=1** | TRL 默认 8 → 每步生成 8 次（~8× 慢）；run-5 显式设 1，回归丢了。修后 725s → 242s/step |
| est_peak 正确分片 | GRPO 单卡需 ~23GB（OOM 24GB 卡）→ 必须分片；估计 25.7GB@768 正确触发分片 |
| grpo_hidden 独立 ckpt | 原复用 grpo_proxy 的 ckpt → 消融失效；改用 `grpo_hidden_only_ckpt` |
| in-training canary 精准化 | abort 仅在 validity==0 **且** eos_frac==0（真 EOS bug），防 5 样本探针误杀健康条件 |

生成器（durable，进 git）commit：`464804d` … `6fb6b4b` → `e2713b6` → `f304c93`（共 12 个）。产物 main.py 手改同步（artifact 不入库）。

---

## 5. 预期运行 + 早警设施

**预测 ~44h**（实测步时外推）：

| 部分 | 时长 |
|---|---|
| SFT（6 个：3 臂 × 2 seed，8s/step × 939 step） | ~12.5h |
| GRPO（4 个：242s/step 分片 × 100 step） | ~27h |
| proxy 采集（2 个，7.7s/样本 × 500） | ~2.1h |
| eval（8 条件，7.7s/样本 × 100） | ~1.7h |
| scratch + 加载开销 | ~1h |
| **总计** | **~44h** |

STOP 在 56h → **~12h 余量 + 14h 收尾缓冲**，宽松。

**早警/可观测**：in-training canary（epoch 1 抓训练质量）/ reward canary（GRPO 前）/ dead-reward 守卫 / 时间预算优雅降级 / progress.json 心跳 / per-sample 审计 dump。整合 smoke 已端到端验证 8/8 条件 0 崩溃。

---

## 6. 发车命令（直接 nohup）

```bash
cd /root/AutoResearchClaw/artifacts/rc-20260605-140117-fdedd0/stage-12/runs/sandbox/_project_1
nohup env PYTHONUNBUFFERED=1 .venv/bin/python main.py > quality_run.log 2>&1 &
```

监控：progress.json 心跳 / canary / 各条件落盘 / traceback，按整合 smoke 节奏巡检。
