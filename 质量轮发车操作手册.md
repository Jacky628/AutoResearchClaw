# 质量轮发车操作手册（2026-06-21，A 状态）

run: `rc-20260605-140117-fdedd0` · 跑法 = **直接 nohup 沙盒 main.py**（非 researchclaw run，见 [[cad-llm-artifact-sandbox-sync]]）。

```bash
# 全程的沙盒路径（下文 $SBX）
SBX=/root/AutoResearchClaw/artifacts/rc-20260605-140117-fdedd0/stage-12/runs/sandbox/_project_1
```

---

## 0. 发车前最后确认（每项过一遍）

```bash
cd "$SBX"
# a) smoke 门控必须 OFF（否则 1-epoch 假跑）
echo "ARC_SMOKE='${ARC_SMOKE}'   # 必须为空"
# b) 没有残留进程
pgrep -af "bin/python [m]ain\.py" || echo "无残留 ✓"
# c) 关键超参（应：grad_accum=8 epochs=3 max_seq=1024 grpo_steps=100 num_tokens=5 prefix=0.40 budget=252000/0.80）
grep -nE '"sft_grad_accum"|"sft_epochs"|"max_seq_length"|"grpo_steps"|"num_constraint_tokens"|"eval_prefix_fraction"|"total_budget_seconds"|"budget_fraction"' main.py
# d) GPU 两卡空闲 + 磁盘
nvidia-smi --query-gpu=index,memory.used,utilization.gpu --format=csv
df -h "$SBX" | tail -1
# e) 全新跑：确认没有旧 partial（若要全新而非续跑）
ls -la partial_results.jsonl checkpoints/ adherence_samples.jsonl 2>/dev/null || echo "干净（全新跑）✓"
```
**全新跑**：上面 partial/checkpoints 应不存在。**续跑**：保留它们，别动。

---

## 1. 启动

```bash
cd "$SBX"
nohup env PYTHONUNBUFFERED=1 .venv/bin/python main.py > quality_run.log 2>&1 &
echo "PID=$!"          # 记下 PID
```

---

## 2. 启动后立即检查（0–15 分钟，冷启动健康）

```bash
cd "$SBX"
pgrep -af "bin/python [m]ain\.py"      # 进程在
tail -f quality_run.log                 # 实时看
```
**期望看到（按顺序）**：
- `DEDUP: train hash sets built (exact=… loose=… over … rows)`
- `DEDUP: test N -> M held-out` ← **去泄漏生效**（M≈9442 干净留出集）
- `CHECKPOINT_RETRAIN: condition=deepcad_scratch_transformer seed=0` ← 第一个条件开训
- **无 Traceback**；`nvidia-smi` 有利用率。

🚩 红旗：起步即 Traceback / `DEDUP` 没出现 / GPU 0% 利用率 → 立即 `pkill` 排查。

---

## 3. 早期审核（首个 SFT，~2–3h，**最关键的质量关**）

第一个 Qwen SFT 条件（`qwen25coder_sft_no_constraints seed=0`）跑起来后,审这几样:

```bash
cd "$SBX"
grep "CANARY:" quality_run.log | tail            # SFT 训练中 canary
grep -E "validity_rate|adherence_f1|adherence_recall" quality_run.log | tail
wc -l partial_results.jsonl adherence_samples.jsonl 2>/dev/null
```
**逐项判据**：
| 信号 | 健康 | 红旗 |
|---|---|---|
| `CANARY: … eos_frac=0.XX validity=0.XX` | **eos_frac > 0 且随 epoch 升**（EOS 学到了,pad≠eos 修复生效） | `validity==0 AND eos_frac==0`（EOS 没学会,会自动 abort）|
| SFT loss | 平稳下降 | NaN / 爆炸 / 卡住 |
| 首个 eval `validity_rate` | **> 0**（恒零 bug 仍解除） | =0 全程 |
| `adherence_f1` | 有数值（非 nan） | nan（度量没算上）|
| `partial_results.jsonl` | 该条件完成后 +1 行 | 训练完却不写 |
| `adherence_samples.jsonl` | eval 时逐样本累积 | 空 |

> 早期审核过 = 链路 + 修复全部行为级生效,可放心让它长跑。没过 = 趁早停,别等 47h。

---

## 4. GRPO 阶段审核（进入 grpo_* 条件后）

```bash
cd "$SBX"
nvidia-smi --query-gpu=index,memory.used --format=csv   # GRPO@768 应两卡分片(对称占用)
grep -E "REWARD_CANARY|RL_DEAD_REWARD|final proxy-oracle agreement" quality_run.log | tail
```
- **两卡分片**（est_peak 让 GRPO 分片,单卡会 OOM）。
- `REWARD_CANARY` 出现、`RL_DEAD_REWARD` **不**触发（reward 没死）。
- step 时间 ~242s（巡检时估预算节奏）。
- `final proxy-oracle agreement=…` 收尾打印。

---

## 5. 常规巡检（每几小时一次）

```bash
cd "$SBX" && python3 pause_status.py     # 一站式:跑到哪 / 已完成 N/14 / 停了丢多少
# 预算节奏:已耗时 vs STOP 56h,已完成 vs 14
```

---

## 6. 暂停

```bash
cd "$SBX"
python3 pause_status.py                  # 先看「现在停丢多少」
# 最省:某条件刚完成、下一个 CHECKPOINT_RETRAIN 之前停
pkill -f "bin/python [m]ain\.py"
pgrep -af "bin/python [m]ain\.py" || echo "已停 ✓"
```
**铁律**：① 绝不清沙盒（别删 partial_results/checkpoints）；② 停在条件中途丢该条件（SFT≤2h / GRPO≤7h,复用的 SFT 保留）；③ 停在条件间≈0 损失。

---

## 7. 暂停后重新启动（恢复）

```bash
cd "$SBX"
nohup env PYTHONUNBUFFERED=1 .venv/bin/python main.py >> quality_run.log 2>&1 &   # 同命令,>> 追加日志
tail -f quality_run.log
```
**验证恢复生效**：启动应打印
- `[main] Loaded N partial results`
- `[main] Skipping <cond> seed=<s> (already done)` ← 已完成的被跳过
- 从第一个未完成条件接着 `CHECKPOINT_RETRAIN`。

> 注：预算计时器重启归零（STOP 从重启时刻重算 56h,剩余条件拿满预算,够用）。

---

## 8. 全部跑完后

```bash
cd "$SBX"
tail -40 quality_run.log                  # 最终聚合:overall_validity / validity_gap_proxy_vs_oracle_pp
python3 paired_analysis.py                # adherence 配对比较(tokens vs text/masked/none + text vs none)
cat results.json | python3 -m json.tool | less   # 全部结果
```
产出：`results.json`（8 条件 × seed 全指标）、`adherence_samples.jsonl`（逐样本 recall,配对分析用）、checkpoints。

---

## 关键值速查
- STOP at **56h**（70h × 0.80）；预测 ≈47h → ~9h 余量。
- 14 run：scratch×2 → SFT×6 → proxy×2 → GRPO×4 → masked×2（顺序见 pause_status.py）。
- 主度量 **adherence_f1**（次 validity_rate）；prefix=0.40；5-tag 词表。
